import { PageContainer } from "@toolpad/core/PageContainer";
import React from "react";

const Events = () => {
    return (
        <PageContainer>
            <div>events</div>
        </PageContainer>
    );
};

export default Events;
